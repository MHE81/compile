class MyClass inherits MElika begin
    public field double x, y, v
    private field double z
    field string s
    func myMethod(param1: double, param2: double, param3: string) returns bool begin
        var a = 10, b = 20.5, d=75
        var c = love(10)
        if(n<2)
            x = 10
        else
            m++
        
	    mel = 10+c
	    d++
        param1 = s
        c--
        return true
    end
    func myMethod(param1: double, param2: double, param3: string) returns bool begin
        sout("loveyou")
        return true
    end

    func anotherMethod(init: string) returns string begin
        sout("Hello, world!")
	    if (x<5 && b==3)
            s++
	    elif (c==9)
		    v++
	    elif (c>d)
		    d++
        elif (b==10)
            d++
	    else
            m++
        return "Done"
    end
end

MAIN class Amir begin
    field string fati
end

public class MElika inherits Fateme begin
    private field double x, y
    private field double z
    func myMethod(param1: int, param2: double) returns bool begin
        var a = 10, b = 20.5
        var c = a + b
	while(c == true) begin
        while(f==10)
            c++
		continue
        end
        return true
    end

    func anotherMethod(mel: string) returns string begin
        sout("Hello, world!")
	while(mel ==meli)
        if(c==2)
            var b = 3, c=10, v= 2
        else
		    c++
        return "Done"
    end
end

public class Fateme inherits MyClass begin
    public field double x, y
    private field double z

    func myMethod(param1: double, param2: double) returns bool begin
        var a = 10, b = 20.5
        return true
    end

    func anotherMethod(init: double) returns string begin
        sout("Hello, world!")
        return "Done"
    end
    private func a (x: bool [], y: string, z: double) returns string begin
	return b + c
    end
end

