class MyClass begin
    public field int x, y, v
    private field double z

    func myMethod(param1: int, param2: double, param3: string) returns bool begin
        var a = 10, b = 20.5, d=75
        var c = love(10)
	mel = 10+c
	d++
    c--
        return true
    end

    func anotherMethod(init: string) returns string begin
        sout("Hello, world!")
	if (x<5 && b==3) begin
            if(c==9)begin
                v++
                while(c==8)
                    x++
            end
            elif(s==10)
                a++
		c++
        sout("hiiiiiii")
    end
	elif (c==9)
		v++
	elif (c>d)
		d++
    elif (b==10)
        d++
	else begin
		c++
        m++
        end
        return "Done" || true
    end
end

MAIN class Amir begin
    field string fati
end

public class MElika inherits Fateme begin
    public field int x, y
    private field double z

    func myMethod(param1: int, param2: double) returns bool begin
        var a = 10, b = 20.5
        var c = a + b
	while(c == true) begin
        c++
		continue
        end
        return true
    end

    func anotherMethod(mel: string) returns string begin
        sout("Hello, world!")
	while(mel ==meli)
        if(c==2)
            var b = 3, c=10, v= 2
        else
		    c++
        return "Done"
    end
end

class Fateme begin
    public field int x, y
    private field double z

    func myMethod(param1: int, param2: double) returns bool begin
        var a = 10, b = 20.5
        return true
    end

    func anotherMethod(init: int) returns string begin
        sout("Hello, world!")
        return "Done"
    end
    private func a (x: bool [], y: string, z: double) returns string begin
	return b + c
    end
end

